# Blockchain Advisor - Extended Requirements
python_version >= "3.8"

# Optional enhancements (not required for basic functionality)
colorama==0.4.6         # for colorful terminal output
tabulate==0.9.0         # for formatting tables or summaries
rich==13.9.1            # for rich text UI in terminal
